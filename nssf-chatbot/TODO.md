# TODO

- [x] Inspect existing chat UI code (backend and frontend).
- [x] Update frontend rendering so chatbot output is readable with basic structure (paragraphs, bullet/numbered lists, headings, blockquotes).
- [x] Fix `chat.js` syntax/escaping issues reported by the editor (unterminated string literal in `escapeHtml`).

- [ ] Update `chat.css` to style rendered structured elements inside message bubbles.
- [ ] Smoke-test the UI in browser: verify list rendering and headings in assistant messages.

